[
  {
    "id": "SDS-326",
    "epic": "SDS",
    "title": "Rewrite A/B Testing Platform teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored A/B Testing Platform at F. Add teachingContent section with: (1) Hook: \"Netflix runs 250+ A/B tests simultaneously, each affecting millions of users. One misconfigured experiment cost a streaming company 5% of subscriptions in a single week. How do you build a platform that guarantees statistical rigor while shipping features fast?\", (2) Analogy: \"Like a clinical drug trial — you split patients into control and treatment groups, carefully measure outcomes, and only approve the drug if the results are statistically significant, not just because one group felt better.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Sample ratio mismatch (SRM) — when the 50/50 split drifts to 48/52 due to bot traffic or caching, invalidating all statistical conclusions\", \"Interaction effects — two simultaneous experiments modify the same feature and their combined effect cancels out the individual signals\"], (5) Common mistakes: [\"Peeking at results before reaching statistical significance and calling the test early — this inflates false positive rates from 5% to over 25%\", \"Using user-level randomization for session-level metrics, causing the same user to see both variants across devices and contaminating results\"], (6) Summary: [\"Randomize at the right granularity (user vs session vs request) and never peek at results early\", \"Guard against SRM by monitoring split ratios in real-time and auto-pausing experiments that drift\", \"Isolate experiments using layers/namespaces so concurrent tests don't interact\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/ab-testing-platform.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-327",
    "epic": "SDS",
    "title": "Rewrite Distributed API Rate Limiter teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Distributed API Rate Limiter at F. Add teachingContent section with: (1) Hook: \"GitHub's API serves 3 billion requests per day. Without rate limiting, a single runaway script could consume all capacity and bring the platform down for 100 million developers. How do you enforce limits across 50+ globally distributed servers without a single bottleneck?\", (2) Analogy: \"Like a nightclub bouncer with a clicker counter — each person entering increments the count, and once capacity is hit, everyone waits outside regardless of which door they approach.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Clock skew between distributed nodes causes a user to get 2x their allowed rate by hitting two servers whose sliding windows are out of sync\", \"Thundering herd at window boundary — all blocked clients retry simultaneously when a new rate limit window opens, causing a spike larger than the original limit\"], (5) Common mistakes: [\"Using a fixed-window counter instead of sliding window, allowing 2x burst at window boundaries when a user sends max requests at 11:59 and again at 12:00\", \"Storing counters only in local memory per server instance — a user hitting N different servers behind a load balancer gets N times their rate limit\"], (6) Summary: [\"Sliding window log or counter eliminates the 2x burst problem at window boundaries\", \"Centralized counter (Redis) is simplest; local counters with gossip trade accuracy for latency\", \"Always return Retry-After headers so well-behaved clients back off instead of hammering\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/api-rate-limiter.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-328",
    "epic": "SDS",
    "title": "Rewrite Authentication Service teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Authentication Service at F. Add teachingContent section with: (1) Hook: \"The 2023 Okta breach exposed 5,000 companies because one stolen session token bypassed all password protections. Authentication isn't just login — it's the single gate that guards every API call, every microservice, every piece of user data. How do you design it so a breach in one layer doesn't cascade everywhere?\", (2) Analogy: \"Like an airport security checkpoint — your passport (credentials) gets verified once at the gate, then you receive a boarding pass (token) that every subsequent checkpoint trusts without re-checking your passport.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Token replay attack — an attacker intercepts a valid JWT and reuses it from a different device, gaining full access until expiry\", \"Clock skew between auth server and API servers causes tokens to be rejected as expired even though they were just issued 2 seconds ago\"], (5) Common mistakes: [\"Storing JWTs in localStorage instead of httpOnly cookies, making them vulnerable to XSS attacks that steal tokens with a single script injection\", \"Setting token expiry to 24 hours or longer to reduce login friction, but creating a massive window for stolen token abuse — use short-lived access tokens with refresh token rotation instead\"], (6) Summary: [\"Short-lived access tokens (15 min) + refresh token rotation = minimal blast radius on theft\", \"Never store tokens in localStorage — use httpOnly, secure, sameSite cookies\", \"Centralized auth with distributed verification (public key JWT validation at each service) avoids a single point of failure\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/authentication-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-329",
    "epic": "SDS",
    "title": "Rewrite WhatsApp-style Chat teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored WhatsApp-style Chat at F. Add teachingContent section with: (1) Hook: \"WhatsApp delivers 100 billion messages per day to 2 billion users, and every single one must arrive in order, exactly once, even when the recipient is offline in a rural village with 2G connectivity. How do you guarantee delivery at that scale without losing a single message?\", (2) Analogy: \"Like a post office with guaranteed delivery — if the recipient isn't home, the letter waits at the post office (message queue), and a delivery receipt is sent back to the sender so they know it arrived.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Split-brain in message ordering — User A sends messages M1 then M2, but due to network partitions, the server receives M2 before M1, displaying them out of order on the recipient's screen\", \"Offline message flood — a user comes online after 3 days offline and 10,000 pending messages need to be synced without overwhelming their device or the connection\"], (5) Common mistakes: [\"Using server timestamps for message ordering instead of vector clocks or Lamport timestamps, causing messages to appear out of order when clients have clock drift\", \"Polling for new messages instead of maintaining persistent WebSocket connections, wasting 90% of requests on empty responses and adding 1-3 seconds of latency per message\"], (6) Summary: [\"Persistent connections (WebSocket/MQTT) + offline queue = messages never lost, always delivered\", \"Use client-generated monotonic IDs for ordering, not server timestamps — clocks lie\", \"Fan-out on read for group chats (store once, deliver per-member) keeps write amplification manageable\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/chat-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-330",
    "epic": "SDS",
    "title": "Rewrite CI/CD Pipeline teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored CI/CD Pipeline at F. Add teachingContent section with: (1) Hook: \"Google deploys 800,000 builds per day across 2 billion lines of code. A single broken deploy to production can cost $1M per minute in lost revenue. How do you design a pipeline that moves this fast without ever shipping a broken build to users?\", (2) Analogy: \"Like an assembly line in a car factory — each station (build, test, stage, deploy) inspects the product, and if any station finds a defect, the line stops immediately rather than shipping a broken car to the dealer.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Flaky tests — a test passes 95% of the time but fails randomly, causing the pipeline to block legitimate merges and eroding developer trust in the system\", \"Dependency diamond — Service A deploys first and changes an API contract, then Service B deploys and breaks because it depends on the old contract, but both passed CI independently\"], (5) Common mistakes: [\"Running all tests sequentially in a single job instead of parallelizing by test suite, turning a 5-minute pipeline into a 45-minute bottleneck that blocks the entire team\", \"Deploying directly to production without a canary or blue-green phase, meaning a bug in one commit affects 100% of users instead of being caught at 1% traffic\"], (6) Summary: [\"Parallelize test suites and cache dependencies — pipeline speed directly affects developer velocity\", \"Canary deployments catch production-only bugs that no test environment can replicate\", \"Immutable artifacts (Docker images, not 'latest' tags) ensure what you tested is exactly what you deploy\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/ci-cd-pipeline.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-331",
    "epic": "SDS",
    "title": "Rewrite Collaborative Editor teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Collaborative Editor at F. Add teachingContent section with: (1) Hook: \"Google Docs supports 100 simultaneous editors in a single document, each typing at 5 characters per second. Every keystroke must appear on every other user's screen within 200ms, and no edit can ever be lost — even when two people type in the same word at the same instant. How?\", (2) Analogy: \"Like a shared whiteboard where everyone has their own marker — if two people write in the same spot simultaneously, the board must figure out how to merge both contributions without erasing either one, like interleaving their letters.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Concurrent delete and insert at the same position — User A deletes a paragraph while User B inserts a sentence in the middle of it, and the system must decide whether the insertion survives or vanishes with the deletion\", \"Network partition during active editing — a user continues editing offline for 10 minutes, generating hundreds of operations that must all be reconciled when they reconnect without overwriting other users' changes\"], (5) Common mistakes: [\"Choosing OT (Operational Transform) without understanding it requires a central server for total ordering — it cannot work in a peer-to-peer architecture, unlike CRDTs which converge without coordination\", \"Broadcasting full document state on every change instead of sending only the delta operations, causing bandwidth to scale with document size rather than edit frequency\"], (6) Summary: [\"CRDTs converge without central coordination; OT is simpler but requires a single ordering server\", \"Send operations (insert/delete at position), never full document snapshots — bandwidth must scale with edit rate, not doc size\", \"Cursor presence (who is editing where) requires a separate fast channel since it updates 10x more frequently than actual edits\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/collaborative-editor.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-332",
    "epic": "SDS",
    "title": "Rewrite Content Moderation Pipeline teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Content Moderation Pipeline at F. Add teachingContent section with: (1) Hook: \"YouTube receives 500 hours of video every minute. A single viral harmful video can reach 10 million views in 2 hours. You need to catch it before it spreads — but you also can't flag 1% of legitimate content as harmful, or creators will revolt. How do you build moderation that's both fast and accurate?\", (2) Analogy: \"Like airport luggage screening — every bag goes through an automated X-ray (ML model) first, and only suspicious items get pulled aside for a human inspector to open and examine manually.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Adversarial content — users slightly modify banned images (add noise, mirror, overlay text) to evade hash-matching and ML classifiers while keeping the content recognizable to humans\", \"Context-dependent moderation — the same image of a medical procedure is educational in a health forum but graphic content on a children's platform, requiring per-community policy rules\"], (5) Common mistakes: [\"Running all content through a single synchronous ML pipeline, creating a bottleneck where upload latency spikes to 30+ seconds during traffic surges instead of using async processing with optimistic publishing\", \"Treating moderation as a binary allow/block decision instead of a confidence spectrum — content at 0.4-0.7 confidence should go to human review, not be auto-blocked at 0.5 threshold\"], (6) Summary: [\"Layered pipeline: hash match (instant) -> ML classifier (seconds) -> human review (minutes) handles volume efficiently\", \"Optimistic publish + async moderation keeps upload latency low; retroactive takedown handles false negatives\", \"Track appeal rates and false-positive rates as core metrics — over-moderation is as damaging as under-moderation\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/content-moderation.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-333",
    "epic": "SDS",
    "title": "Rewrite Data Lake Architecture teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Data Lake Architecture at F. Add teachingContent section with: (1) Hook: \"Uber's data lake ingests 100 petabytes and processes 1 million queries per day across 20,000 analysts. Without proper architecture, that data lake becomes a 'data swamp' — everyone dumps data in, nobody can find anything, and queries that should take seconds take hours. How do you keep it organized at petabyte scale?\", (2) Analogy: \"Like a massive warehouse with three zones: the loading dock (raw/bronze) where deliveries arrive untouched, the sorting floor (silver) where items are cleaned and categorized, and the showroom (gold) where curated products are displayed ready for customers.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Schema evolution — a producer changes a field from integer to string, breaking every downstream pipeline that assumed the original type, cascading failures across hundreds of jobs\", \"Late-arriving data — events from mobile devices arrive hours or days late due to offline usage, requiring reprocessing of partitions that were already marked as complete\"], (5) Common mistakes: [\"Storing all data in a single flat namespace without partitioning by date/source, causing full table scans on every query and making a 10-second query take 3 hours on petabyte data\", \"Skipping the schema registry and allowing producers to write any format, so consumers discover schema changes only when their jobs crash at 3 AM\"], (6) Summary: [\"Bronze/Silver/Gold layering separates ingestion concerns from analytics concerns — never query raw data directly\", \"Partition by date and source; use columnar formats (Parquet) for 10-100x query speedup over row-based CSV/JSON\", \"Schema registry + compatibility checks prevent producers from breaking downstream consumers\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/data-lake.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-334",
    "epic": "SDS",
    "title": "Rewrite Discord Real-Time Messaging teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Discord Real-Time Messaging at F. Add teachingContent section with: (1) Hook: \"Discord serves 150 million monthly active users across 19 million active servers. A single popular server can have 500,000 members, and when someone posts in #general, that message must reach every online member within 100ms. How do you fan out a single message to half a million recipients in real-time without melting your servers?\", (2) Analogy: \"Like a stadium PA system with zone speakers — instead of one giant speaker trying to reach everyone (fan-out on write), the announcement goes to zone controllers (guild shards), and each zone speaker broadcasts only to the people in its section who are actually listening.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Guild shard hotspot — a viral event causes 500K users to join one server simultaneously, overwhelming the single shard responsible for that guild while other shards sit idle\", \"Message ordering across shards — a user in two channels on different shards sees messages arrive out of global order because each shard has its own sequence counter\"], (5) Common mistakes: [\"Using a single WebSocket connection pool for all guilds instead of sharding by guild ID, meaning one large server's traffic degrades connection quality for users in small, quiet servers\", \"Sending full message objects (with author info, embeds, attachments) on every message instead of sending references and letting clients cache author/embed data, multiplying bandwidth by 5-10x\"], (6) Summary: [\"Shard by guild ID so each server's traffic is isolated — hot guilds can be split further if needed\", \"Lazy-load channel history (fan-out on read) but push new messages in real-time (fan-out on write) — hybrid approach\", \"Gateway servers manage WebSocket connections separately from business logic servers to scale connection count independently\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/discord.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-335",
    "epic": "SDS",
    "title": "Rewrite Distributed Cache teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Distributed Cache at F. Add teachingContent section with: (1) Hook: \"Facebook's Memcached cluster serves 5 billion requests per second across thousands of nodes. Remove the cache and their database would need 500x more servers to handle the load. But a single cache node failure during peak traffic can trigger a cascading database overload that takes down the entire site. How do you design a cache that's both fast and resilient?\", (2) Analogy: \"Like having a sticky note on your desk with frequently dialed phone numbers — instead of looking up each number in the phone book (database) every time, you glance at the note. But if the note falls off, you need the phone book to still work without being overwhelmed by everyone calling directory assistance at once.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Cache stampede — a popular cached item expires, and 10,000 concurrent requests all miss the cache simultaneously and hit the database, causing a spike that can cascade into a full outage\", \"Stale read after write — a user updates their profile, the database write succeeds, but the cache still has the old value, so the user sees their own update disappear until TTL expires\"], (5) Common mistakes: [\"Using a single cache node without replication, creating a single point of failure where one node crash causes instant 100% cache miss rate and database overload\", \"Setting the same TTL for all cache entries instead of adding jitter — when millions of entries expire at exactly the same second, it creates a thundering herd identical to a cold cache start\"], (6) Summary: [\"Consistent hashing distributes keys evenly and minimizes redistribution when nodes join/leave\", \"Protect against stampede with request coalescing (single-flight), probabilistic early expiry, or lock-based recomputation\", \"Cache-aside is simplest; write-through ensures consistency but adds write latency — choose based on your read:write ratio\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/distributed-cache.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-336",
    "epic": "SDS",
    "title": "Rewrite Distributed KV Store teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Distributed KV Store at F. Add teachingContent section with: (1) Hook: \"DynamoDB serves 89 million requests per second for Amazon during Prime Day. Every millisecond of latency costs real revenue. But the data is spread across thousands of nodes — how do you guarantee a key lookup returns the right value in under 5ms when the node holding it might have just crashed 2 seconds ago?\", (2) Analogy: \"Like a library system with multiple branches — each book has copies at several locations (replication), a master catalog says which branches have which books (consistent hashing), and if one branch burns down, the other copies ensure no book is ever lost.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Split-brain during network partition — two nodes both accept writes for the same key with different values, and when the partition heals, the system must reconcile conflicting versions without data loss\", \"Hotkey problem — a single viral item (e.g., celebrity tweet metadata) receives 100x the traffic of normal keys, overwhelming the single node responsible for that key's hash range\"], (5) Common mistakes: [\"Choosing strong consistency (linearizability) by default without understanding it requires synchronous replication to a quorum, adding 5-10ms of latency per write — eventual consistency with conflict resolution is sufficient for 90% of use cases\", \"Implementing naive modulo-based partitioning (key % N) instead of consistent hashing, so adding one node forces 90% of keys to migrate instead of just 1/N\"], (6) Summary: [\"Consistent hashing + virtual nodes distributes load evenly and minimizes rebalancing on cluster changes\", \"Tunable consistency via quorum reads/writes (R + W > N) lets you trade latency for consistency per use case\", \"Vector clocks or last-write-wins resolve conflicts during partition healing — pick the strategy that matches your data semantics\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/distributed-kv.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-337",
    "epic": "SDS",
    "title": "Rewrite DNS Resolution System teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored DNS Resolution System at F. Add teachingContent section with: (1) Hook: \"Every single internet request begins with a DNS lookup. Cloudflare's 1.1.1.1 resolver handles 1.2 trillion queries per day. If DNS goes down, the entire internet appears broken — no websites load, no APIs respond, no emails send. How do you design the world's most critical naming system to never fail?\", (2) Analogy: \"Like asking for directions in a foreign city — you ask a local guide (recursive resolver), who asks the city hall (root server), who points to the neighborhood office (TLD server), who finally points to the exact building (authoritative server). And everyone remembers the directions for a while so they don't keep asking.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"DNS cache poisoning — an attacker races to inject a fake response before the legitimate authoritative server replies, redirecting all traffic for a domain to a malicious server until the TTL expires\", \"Negative caching gone wrong — a misconfigured TTL on an NXDOMAIN response causes resolvers to cache 'this domain doesn't exist' for hours even after the domain is actually fixed\"], (5) Common mistakes: [\"Setting DNS TTL too high (24 hours) to reduce query load, but then being unable to failover traffic quickly during an outage — a 5-minute TTL costs slightly more queries but enables rapid disaster recovery\", \"Not implementing DNS prefetching for known navigation paths, forcing the browser to do a cold DNS lookup on every link click, adding 50-200ms of latency to every page transition\"], (6) Summary: [\"Hierarchical delegation (root -> TLD -> authoritative) distributes the naming system so no single server needs to know everything\", \"Caching at every level (browser, OS, ISP resolver) turns a 200ms lookup chain into a <1ms local hit for 90%+ of queries\", \"DNSSEC prevents cache poisoning by cryptographically signing responses, but adds latency — use it for critical domains\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/dns-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-338",
    "epic": "SDS",
    "title": "Rewrite Email Delivery Service teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Email Delivery Service at F. Add teachingContent section with: (1) Hook: \"Gmail processes 121 billion emails per day. SendGrid delivers 150 billion emails per month for companies like Uber and Airbnb. But here's the hard part: even if your infrastructure is perfect, major ISPs will silently drop your emails if your sender reputation score falls below their threshold. Deliverability is harder than sending. How do you design a system where emails actually arrive?\", (2) Analogy: \"Like a postal service with reputation scores — if a post office (IP address) has been sending junk mail, the receiving post offices start throwing away everything from that address, even legitimate letters. You need multiple clean post offices and a good track record.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"IP reputation collapse — a single spam complaint spike causes an ISP to blacklist your sending IP, and all emails from that IP start going to spam for every recipient on that ISP\", \"Bounce storm — sending to a purchased list with 40% invalid addresses triggers a bounce rate that ISPs interpret as spamming, permanently damaging sender reputation\"], (5) Common mistakes: [\"Sending all email types (transactional receipts, marketing newsletters, password resets) from the same IP/domain, so a marketing campaign that triggers spam complaints also causes password reset emails to land in spam\", \"Not implementing exponential backoff on soft bounces (temporary failures), hammering the receiving server repeatedly and getting the sending IP permanently blocked\"], (6) Summary: [\"Separate transactional and marketing email onto different IPs/domains so reputation issues don't cross-contaminate\", \"Warm up new IPs gradually (start with 100 emails/day, not 1M) — ISPs trust senders who ramp up slowly\", \"Implement feedback loops (FBL), SPF, DKIM, and DMARC — without all four, major ISPs will deprioritize your mail\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/email-service.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-339",
    "epic": "SDS",
    "title": "Rewrite Event Sourcing/CQRS teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Event Sourcing/CQRS at F. Add teachingContent section with: (1) Hook: \"When a bank needs to know not just your current balance but exactly how you got there — every deposit, withdrawal, and transfer in order — a simple CRUD database falls apart. Event sourcing records every state change as an immutable event, giving you a complete audit trail and the ability to reconstruct any past state. But the trade-offs are real. How do you implement it without drowning in complexity?\", (2) Analogy: \"Like a version-controlled ledger — instead of erasing and rewriting your bank balance on every transaction (CRUD), you append each transaction to a permanent log. Your current balance is just the sum of all transactions, and you can always 'rewind' to see what it was at any point in time.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Event schema evolution — you need to add a new field to an event type, but 500 million historical events don't have that field, and replaying them through the new handler crashes\", \"Projection lag — the read model is 30 seconds behind the write model during high load, so a user creates an order and immediately searches for it but gets 'not found'\"], (5) Common mistakes: [\"Applying event sourcing to every service in the system instead of only to domains with complex state transitions or audit requirements, adding massive complexity to simple CRUD services that don't benefit from it\", \"Not implementing snapshots for aggregates with long event histories — replaying 10 million events to reconstruct current state takes minutes instead of the milliseconds a snapshot provides\"], (6) Summary: [\"Event sourcing = append-only log of state changes; CQRS = separate write model (events) from read model (projections)\", \"Use snapshots every N events to avoid replaying entire history — balance snapshot frequency against storage cost\", \"Eventual consistency between write and read sides is inherent — design the UI to handle projection lag gracefully\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/event-sourcing.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-340",
    "epic": "SDS",
    "title": "Rewrite Feature Flag System teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Feature Flag System at F. Add teachingContent section with: (1) Hook: \"Facebook ships code to production 1,000 times per day, but most new features are invisible to users — hidden behind feature flags. This decouples deployment from release, meaning you can deploy code on Monday and enable it for 1% of users on Wednesday. But a misconfigured flag took down Facebook for 6 hours in 2021. How do you build a flag system that's both flexible and safe?\", (2) Analogy: \"Like light switches in a building — each switch controls a room's lights independently. You can install new wiring (deploy code) without flipping the switch (enabling the feature), and if a bulb explodes, you flip the switch off instantly without rewiring anything.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Flag evaluation dependency chain — Flag B depends on Flag A being enabled, but Flag A gets disabled, causing Flag B to enter an undefined state that neither the on nor off code path handles\", \"Stale flag cache — a flag is disabled in the control plane, but edge servers cache the old 'enabled' state for 60 more seconds, continuing to serve the broken feature to users during an incident\"], (5) Common mistakes: [\"Never cleaning up old feature flags after full rollout, accumulating hundreds of dead code paths that make the codebase unmaintainable and create combinatorial testing nightmares\", \"Evaluating flags on the server side for every request instead of caching the evaluation result, adding 5-10ms of latency per request to call the flag service\"], (6) Summary: [\"Decouple deploy from release — ship code dark, enable gradually, kill-switch instantly\", \"Cache flag evaluations locally with short TTL (10-30s); use streaming updates for emergency kill switches\", \"Implement flag lifecycle management — every flag must have an owner, expiry date, and cleanup ticket\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/feature-flags.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-341",
    "epic": "SDS",
    "title": "Rewrite Food Delivery teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Food Delivery at F. Add teachingContent section with: (1) Hook: \"DoorDash handles 25 million deliveries per month across 300,000 restaurants. At lunch rush, the system must match 500,000 simultaneous orders to available drivers within 30 seconds each, while dynamically adjusting ETAs as traffic conditions change every minute. One wrong assignment cascades — cold food, angry customer, lost driver earnings. How do you optimize this three-sided marketplace in real-time?\", (2) Analogy: \"Like a wedding seating planner on fast-forward — you're constantly matching guests (orders) to tables (drivers) based on proximity, preference, and timing, but guests keep arriving, tables keep moving, and the entire venue layout changes every 60 seconds.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Driver cancellation after food pickup — the food is already prepared and in a bag, the driver drops the order, and a new driver must be dispatched immediately, but the food is getting cold and the customer's ETA just doubled\", \"Restaurant overwhelm — a popular restaurant receives 50 orders in 5 minutes, far exceeding its kitchen capacity, and estimated prep times become wildly inaccurate, cascading into late deliveries for all 50 customers\"], (5) Common mistakes: [\"Using simple nearest-driver assignment instead of considering driver direction of travel, batching opportunities, and restaurant prep time — the closest driver heading away from the restaurant is worse than a slightly farther driver heading toward it\", \"Computing ETAs using static distance instead of real-time traffic data, promising 25-minute delivery in rush hour when actual time is 45 minutes, destroying customer trust\"], (6) Summary: [\"Three-sided marketplace (customer, restaurant, driver) requires optimizing all three simultaneously, not just one\", \"Geospatial indexing (S2/H3 cells) + real-time driver location streaming enables sub-second matching at scale\", \"Dynamic ETA = prep time + pickup travel + dropoff travel, all continuously recalculated with live traffic data\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/food-delivery.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-342",
    "epic": "SDS",
    "title": "Rewrite Multiplayer Game Backend teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Multiplayer Game Backend at F. Add teachingContent section with: (1) Hook: \"Fortnite supports 100 players in a single match, each sending position updates 20 times per second. That's 2,000 state updates per second per match, across 5 million concurrent matches. A 50ms delay means you got shot around a corner. How do you synchronize a shared world state for millions of players where every millisecond matters?\", (2) Analogy: \"Like a live TV broadcast with audience interaction — the server is the director maintaining the 'official' version of reality, and each player's screen is a slightly delayed local copy that predicts what will happen next and corrects itself when the director's version disagrees.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Client-side prediction rollback — a player's local prediction of movement diverges from the server's authoritative state, causing the player to visibly snap back to a previous position (rubber-banding) when the correction arrives\", \"Clock synchronization failure — two players shoot each other simultaneously, but server-side clock differences cause one shot to register and the other to be rejected, feeling unfair to the losing player\"], (5) Common mistakes: [\"Making the client authoritative over game state (trusting the client's position/health values) instead of the server, allowing trivial cheats like teleportation and god mode that ruin the experience for everyone\", \"Broadcasting full world state to every player every tick instead of using interest management (only send data about nearby entities), wasting 90% of bandwidth on information the player can't see\"], (6) Summary: [\"Server-authoritative state prevents cheating; client-side prediction + reconciliation hides latency\", \"Interest management / spatial partitioning ensures each player only receives updates about their visible area\", \"Tick rate (20-60 Hz) determines the trade-off between responsiveness and bandwidth — match it to your game genre\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/gaming-backend.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-343",
    "epic": "SDS",
    "title": "Rewrite Google Search teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Google Search at F. Add teachingContent section with: (1) Hook: \"Google processes 8.5 billion searches per day across an index of hundreds of billions of web pages. Each query must return ranked results in under 200ms, even though the index is spread across millions of servers in dozens of data centers. The pages change constantly — 10% of the web is new or modified every week. How do you search the entire internet in a fifth of a second?\", (2) Analogy: \"Like a library with a million librarians — instead of one person searching every shelf, the query is sent to thousands of librarians simultaneously, each responsible for a small section of books. They each find their best matches and send them to a head librarian who merges and ranks the final top 10.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Index freshness vs completeness — a breaking news article published 30 seconds ago needs to appear in results, but the main index only updates every few hours, requiring a separate real-time index for fresh content\", \"Query ambiguity — 'apple' could mean the fruit, the company, or the record label, and the system must infer intent from context, location, and search history without explicit disambiguation\"], (5) Common mistakes: [\"Building a single monolithic inverted index instead of sharding by document ID and querying shards in parallel, meaning search latency scales linearly with index size instead of remaining constant\", \"Ranking purely by keyword relevance (TF-IDF) without considering page authority (PageRank), link structure, and user engagement signals, returning SEO-spammed pages over genuinely useful content\"], (6) Summary: [\"Inverted index sharded across thousands of machines + parallel scatter-gather = constant-time search regardless of index size\", \"Two-phase ranking: fast rough scoring on thousands of candidates, then expensive ML re-ranking on top 1000\", \"Freshness layer (real-time index) supplements the main index for time-sensitive queries — not everything needs to be fresh\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/google-search.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-344",
    "epic": "SDS",
    "title": "Rewrite Hotel Reservation teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Hotel Reservation at F. Add teachingContent section with: (1) Hook: \"Booking.com lists 28 million rooms across 2.5 million properties. During holiday season, 1,000 people might try to book the last available room at the same hotel in the same 10-second window. If two of them both see 'available' and both click 'book,' you've oversold a room that doesn't exist. How do you handle inventory at this concurrency without destroying the booking experience?\", (2) Analogy: \"Like selling the last concert ticket — 500 fans are all clicking 'buy' simultaneously. Only one can win, but you can't show 'sold out' too early (lost revenue) or too late (double-booking). You need a velvet rope (lock) that lets exactly one person through at a time for that specific seat.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Phantom availability — a room shows as available during search but is booked by the time the user completes the 3-minute checkout process, causing a frustrating failure at payment\", \"Overlapping date ranges — a guest books May 5-8 and another books May 7-10 for the same room, and naive date-based queries miss the overlap because neither booking fully contains the other\"], (5) Common mistakes: [\"Using optimistic locking alone for high-contention inventory, causing 99 out of 100 concurrent bookings to fail with retry errors — pessimistic locking or reservation holds (temporary locks with TTL) provide a better UX for the last few rooms\", \"Caching room availability aggressively and serving stale counts, showing '3 rooms left' when actually 0 remain, leading to a surge of failed bookings and customer complaints\"], (6) Summary: [\"Pessimistic locking or reservation holds (5-minute TTL) for last-room scenarios; optimistic locking works when contention is low\", \"Date-range overlap detection requires proper interval queries (start < other.end AND end > other.start), not simple equality checks\", \"Show real-time availability for low-inventory properties, cached availability for high-inventory ones — vary the strategy by scarcity\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/hotel-reservation.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-345",
    "epic": "SDS",
    "title": "Rewrite Image Processing CDN teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Image Processing CDN at F. Add teachingContent section with: (1) Hook: \"Pinterest serves 300 billion images per month. Every image needs to be available in 20+ sizes — from a 50px thumbnail on mobile to a 4K display on desktop. Generating all sizes upfront for 240 billion Pins would cost petabytes of storage. Generating on-the-fly adds latency. How do you serve the perfect image size instantly without bankrupting your storage budget?\", (2) Analogy: \"Like a fast-food chain kitchen — you don't pre-cook every possible order combination (that would waste food), but you do pre-prep popular ingredients (popular image sizes cached at edge). Unusual orders get cooked on demand and then the recipe is saved in case someone orders it again.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Origin shield thundering herd — a new viral image is requested in 50 different sizes from 200 edge locations simultaneously, and all 200 miss the cache and hit the origin server at once to generate 10,000 image variants\", \"WebP/AVIF content negotiation failure — the CDN serves a WebP image to a Safari version that doesn't support it, showing a broken image icon instead of gracefully falling back to JPEG\"], (5) Common mistakes: [\"Generating all possible image variants eagerly on upload (20 sizes x 3 formats = 60 variants per image), wasting enormous compute and storage on sizes that may never be requested\", \"Not setting proper Cache-Control headers with Vary on Accept, causing the CDN to serve a WebP cached response to a browser that only supports JPEG because the URL is identical\"], (6) Summary: [\"Lazy generation + edge caching: generate each size/format on first request, cache at edge, popular variants get served from cache 99%+ of the time\", \"Use Vary: Accept headers and content negotiation to serve optimal format (AVIF > WebP > JPEG) per browser without separate URLs\", \"Origin shield (single intermediate cache) collapses duplicate requests from all edge locations into one generation request\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/image-cdn.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-346",
    "epic": "SDS",
    "title": "Rewrite Instagram teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Instagram at F. Add teachingContent section with: (1) Hook: \"Instagram has 2 billion monthly active users who upload 100 million photos and 500 million Stories per day. When Kylie Jenner posts a photo, it must appear in the feeds of her 400 million followers within seconds. Generating 400 million individual feed entries per post would melt any database. How do you build a feed system that handles celebrity-scale fan-out without collapsing?\", (2) Analogy: \"Like a newspaper with both a printing press and a newsstand — for major headlines (celebrity posts), copies are printed and delivered to every subscriber's doorstep (push/fan-out on write). For niche columnists (small accounts), subscribers pick up the column when they visit the newsstand (pull/fan-out on read).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Celebrity-fan-out problem — a user with 400M followers posts, and fan-out on write would require inserting 400M rows, taking minutes and creating massive write amplification that delays all other users' feed updates\", \"Feed ranking staleness — the ML ranking model scores posts at generation time, but by the time a user opens the app 2 hours later, engagement signals have changed and the ranking is stale\"], (5) Common mistakes: [\"Using pure fan-out on write for all users regardless of follower count, so a single celebrity post creates 400M write operations that overwhelm the database and delay everyone else's feed\", \"Storing full image data in the feed database instead of just post IDs with CDN URLs, bloating feed storage 1000x and making feed queries return megabytes instead of kilobytes\"], (6) Summary: [\"Hybrid fan-out: push for users with <10K followers (fast reads), pull for celebrities (avoid write explosion), merge at read time\", \"Feed = list of post IDs ranked by ML model; actual media served from CDN with signed URLs\", \"Stories use a separate timeline with 24-hour TTL — design for deletion as a first-class operation, not an afterthought\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/instagram.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-347",
    "epic": "SDS",
    "title": "Rewrite E-Commerce Inventory teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored E-Commerce Inventory at F. Add teachingContent section with: (1) Hook: \"Amazon processes 4,000 orders per second during Prime Day. A hot product might have 50 units left with 10,000 people trying to buy it simultaneously. If you oversell, you ship air. If you're too conservative with locks, you show 'sold out' while inventory sits unbought. How do you manage inventory that's accurate to the unit at massive concurrency?\", (2) Analogy: \"Like a popular bakery with a number display showing remaining croissants — the count must decrement atomically as each customer buys, and if two customers grab the last croissant simultaneously, exactly one must be told 'sorry, sold out' immediately, not after they've already paid.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Reservation timeout race — a user adds the last item to cart (reserving it), then abandons checkout. The 15-minute reservation hold expires, but during the release, another user grabs it, causing a brief moment where count shows -1 before correction\", \"Multi-warehouse split fulfillment — an order for 5 units can only be fulfilled by combining 3 from warehouse A and 2 from warehouse B, requiring distributed coordination across inventory databases\"], (5) Common mistakes: [\"Using SELECT then UPDATE (check-then-act) instead of atomic decrement with a WHERE quantity >= requested clause, creating a race condition where two concurrent requests both see quantity=1 and both succeed, overselling by 1\", \"Treating inventory as a single global counter instead of partitioning by warehouse/region, forcing all inventory operations through a single database row that becomes a global bottleneck\"], (6) Summary: [\"Atomic conditional updates (UPDATE SET qty = qty - N WHERE qty >= N) eliminate check-then-act race conditions\", \"Reservation pattern with TTL: hold inventory for checkout duration, auto-release on timeout\", \"Partition inventory by warehouse — aggregate availability across locations for display, but decrement at the specific warehouse level\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/inventory-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-348",
    "epic": "SDS",
    "title": "Rewrite IoT Data Platform teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored IoT Data Platform at F. Add teachingContent section with: (1) Hook: \"Tesla collects telemetry from 5 million vehicles, each sending 100+ sensor readings per second. That's 500 million data points per second — and every single one must be ingested, stored, and queryable within seconds so that anomaly detection can trigger an over-the-air safety recall before a driver is at risk. How do you build a pipeline that never drops data at this ingestion rate?\", (2) Analogy: \"Like a city's water management system — thousands of sensors in pipes (IoT devices) report pressure readings to local substations (edge gateways), which aggregate and forward to the central water authority (cloud platform) that monitors for leaks and anomalies.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Device clock drift — IoT devices without NTP sync report timestamps that are minutes or hours off, causing time-series queries to miss data or place events in the wrong order\", \"Bursty reconnection storm — after a network outage, 1 million devices all reconnect simultaneously and flush their buffered data, creating a 100x normal ingestion spike that overwhelms the pipeline\"], (5) Common mistakes: [\"Writing each sensor reading as an individual row in a relational database, achieving maybe 10,000 writes/sec instead of using a time-series database with batch ingestion that handles millions of writes/sec\", \"Not implementing edge buffering on the device itself — when the network drops, data is simply lost instead of being stored locally and forwarded when connectivity returns\"], (6) Summary: [\"Time-series databases (InfluxDB, TimescaleDB) with columnar storage are 100x more efficient than RDBMS for sensor data\", \"Edge gateways aggregate and buffer locally, smoothing out connectivity gaps and reducing cloud ingestion volume\", \"Device clock correction via server-side timestamp or NTP is mandatory — never trust device-reported time blindly\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/iot-platform.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-349",
    "epic": "SDS",
    "title": "Rewrite Log Analytics teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Log Analytics at F. Add teachingContent section with: (1) Hook: \"Datadog ingests 40 trillion log events per day from millions of servers. When a production incident strikes at 3 AM, an engineer needs to search 10 billion log lines from the last hour and find the one needle-in-a-haystack error in under 5 seconds. How do you make petabytes of unstructured text searchable in real-time?\", (2) Analogy: \"Like a library's full-text search catalog that updates in real-time — every new book (log line) gets its words indexed the moment it arrives on the shelf, so you can instantly find every book that mentions a specific phrase, across millions of volumes.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Log volume spike during incidents — the very moment you need logs most (production outage), log volume spikes 10-100x as error handlers fire repeatedly, potentially overwhelming the ingestion pipeline and causing log loss during the critical investigation window\", \"Cardinality explosion — a developer logs user IDs or request IDs as indexed fields, creating billions of unique values that blow up the inverted index size and slow queries from seconds to minutes\"], (5) Common mistakes: [\"Indexing every field in every log line instead of selecting only queryable fields, causing index sizes to exceed the raw log data size and making ingestion the bottleneck instead of search\", \"Storing all logs at the same retention and storage tier, keeping 90-day-old debug logs on expensive hot storage instead of tiering to cold storage (S3) after 7 days\"], (6) Summary: [\"Separate hot (last 24h, SSD, fully indexed), warm (7-30d, HDD, partially indexed), and cold (30d+, object storage, query on demand) tiers\", \"Structured logging with consistent schemas makes querying possible — unstructured printf-style logs are nearly unsearchable at scale\", \"Back-pressure and sampling during ingestion spikes protect the pipeline — losing 10% of debug logs is better than losing 100% of all logs\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/log-analytics.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-350",
    "epic": "SDS",
    "title": "Rewrite Distributed Logging teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Distributed Logging at F. Add teachingContent section with: (1) Hook: \"Netflix runs 1,000+ microservices, each generating millions of log lines per minute. When a user reports 'video won't play,' the relevant log entry could be on any of 100,000 servers. Without centralized logging, debugging means SSH-ing into servers one by one — a process that takes hours when you need answers in minutes. How do you aggregate logs from 100K servers in real-time?\", (2) Analogy: \"Like a centralized 911 dispatch center — every police car, ambulance, and fire truck (microservice) radios in their status updates, and the dispatch center (log aggregator) records everything in one place so that when an incident happens, the dispatcher can instantly replay everything that happened.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Log ordering across services — Service A logs 'request received' at T1 and Service B logs 'request forwarded' at T2, but due to clock skew, T2 < T1 in the centralized log, making the causal chain appear reversed\", \"Agent failure on a host — the log shipping agent crashes or falls behind, and logs accumulate on local disk until it fills up, at which point the host starts dropping logs from all services on that machine\"], (5) Common mistakes: [\"Shipping logs synchronously in the request path instead of writing to a local buffer and shipping asynchronously, adding 2-5ms of latency to every request and making the logging pipeline a production dependency\", \"Using unstructured text logs (console.log with string concatenation) instead of structured JSON, making automated parsing and querying across millions of heterogeneous log formats nearly impossible\"], (6) Summary: [\"Write to local buffer, ship asynchronously — logging must never add latency to the request path or become a production dependency\", \"Structured JSON logs with consistent fields (traceId, service, level, timestamp) enable cross-service correlation\", \"Correlation IDs (trace/span IDs) propagated across services are the key to reconstructing distributed request flows\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/logging-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-351",
    "epic": "SDS",
    "title": "Rewrite Metrics & Monitoring teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Metrics & Monitoring at F. Add teachingContent section with: (1) Hook: \"Stripe processes billions of dollars in payments daily. A 1% increase in error rate means millions in failed transactions. But you can't alert on every metric — 10,000 microservices each emitting 500 metrics means 5 million time series. How do you detect the signal in 5 million streams of noise, fast enough to page an engineer before customers even notice?\", (2) Analogy: \"Like a hospital patient monitoring system — each patient (service) has vital signs (metrics) displayed on a central nurse station screen. Normal ranges trigger green lights, but the moment a heartbeat goes irregular, the alarm sounds immediately and a nurse is dispatched, even at 3 AM.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Metric cardinality explosion — a developer adds a userId label to a latency metric, creating one time series per user (100M series), overwhelming the metrics database and causing all dashboards to timeout\", \"Alert fatigue — 200 alerts fire during a cascading failure because each downstream service alerts independently, but they all stem from one root cause, and the on-call engineer can't identify the origin in the noise\"], (5) Common mistakes: [\"Using averages for latency monitoring instead of percentiles (p50, p95, p99) — an average of 100ms can hide the fact that 1% of users experience 5-second latencies that destroy their experience\", \"Alerting on absolute thresholds without accounting for seasonality — traffic naturally drops 60% on weekends, triggering low-traffic alerts every Saturday morning that get ignored, including the one real outage\"], (6) Summary: [\"Use percentiles (p99), not averages — averages hide the worst user experiences behind good ones\", \"Control cardinality: bounded labels only (status_code, region, service) — never user_id, request_id, or any unbounded dimension\", \"Alert on symptoms (error rate, latency) not causes (CPU, memory) — symptoms tell you users are hurting, causes tell you why\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/metrics-monitoring.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-352",
    "epic": "SDS",
    "title": "Rewrite Microservices with Mesh teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Microservices with Mesh at F. Add teachingContent section with: (1) Hook: \"Uber runs 4,000+ microservices. Every user request triggers a chain of 20+ inter-service calls. If each call has 99.9% availability, the overall request success rate drops to 98% — meaning 2 out of every 100 rides fail. Without a service mesh handling retries, circuit breaking, and observability, microservices become a distributed monolith that's harder to debug than the monolith it replaced.\", (2) Analogy: \"Like a city's road infrastructure — the services are buildings, and the service mesh is the system of roads, traffic lights, and GPS navigation between them. Without roads, every building can exist independently, but nothing can communicate. The mesh handles routing, traffic management, and detour planning when a road is blocked.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Sidecar proxy resource overhead — each service instance gets its own Envoy proxy consuming 50MB RAM and adding 1ms latency, and at 10,000 instances, that's 500GB of RAM and a measurable latency tax on every call\", \"Retry amplification — Service A retries 3 times to Service B, which retries 3 times to Service C, creating 9 downstream calls from one original request and amplifying load by 9x during partial failures\"], (5) Common mistakes: [\"Adopting microservices and a service mesh for a team of 5 engineers and a simple CRUD app, adding massive operational complexity for zero architectural benefit — a modular monolith would be 10x simpler\", \"Not implementing circuit breakers, so one slow downstream service causes all upstream services to hold connections open and exhaust thread pools, cascading the failure across the entire system\"], (6) Summary: [\"Service mesh (Istio/Linkerd) extracts cross-cutting concerns (retries, TLS, observability) out of application code and into infrastructure\", \"Circuit breakers prevent cascading failures; retry budgets prevent retry amplification storms\", \"Start with a modular monolith — only extract to microservices when team size and deployment velocity demand it\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/microservices-demo.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-353",
    "epic": "SDS",
    "title": "Rewrite ML Pipeline teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored ML Pipeline at F. Add teachingContent section with: (1) Hook: \"Spotify retrains its recommendation models on 600 million tracks and 500 million users weekly. A single training run processes 100TB of interaction data across 1,000 GPUs. But the model that performs best in offline evaluation sometimes degrades live metrics by 3% due to distribution shift. How do you build a pipeline that trains, validates, and deploys ML models safely at this scale?\", (2) Analogy: \"Like a pharmaceutical manufacturing pipeline — you don't just discover a drug formula (train a model) and ship it to pharmacies. It goes through quality control (validation), a clinical trial (A/B test in production), and only then mass production (full deployment), with the ability to recall it instantly if side effects appear.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Training-serving skew — the feature engineering code used during training computes features differently than the serving code, causing the model to see data distributions in production it never saw during training, silently degrading accuracy\", \"Data pipeline failure mid-training — a training job runs for 8 hours on 1,000 GPUs and crashes at hour 7 due to a corrupt data shard, wasting $50K in compute because no checkpointing was implemented\"], (5) Common mistakes: [\"Deploying a new model directly to 100% of traffic instead of running a shadow deployment or canary rollout, so a regression affects every user before anyone notices the metrics have dropped\", \"Not versioning training data alongside model artifacts, making it impossible to reproduce a model's results or debug why a model trained 3 months ago performed better than today's\"], (6) Summary: [\"Version everything: data, features, model, code, config — reproducibility is non-negotiable for debugging and rollback\", \"Feature stores unify training and serving feature computation, eliminating training-serving skew\", \"Canary deployment with automatic rollback: monitor business metrics for the first hour, not just model accuracy metrics\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/ml-pipeline-infra.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-354",
    "epic": "SDS",
    "title": "Rewrite Monitoring & Alerting teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Monitoring & Alerting at F. Add teachingContent section with: (1) Hook: \"AWS manages millions of servers across 30+ regions. In 2017, a single typo in an S3 automation script took down half the internet for 4 hours. The monitoring system itself was hosted on S3 and went down too. How do you build monitoring that survives the very outages it's supposed to detect?\", (2) Analogy: \"Like a smoke detector system in a skyscraper — each floor has its own detector (agent), alerts go to a central fire panel (alerting service), and the fire panel has battery backup (independent infrastructure) so it keeps working even when the building's power goes out.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Monitoring dependency on monitored system — the alerting pipeline runs on the same Kubernetes cluster it monitors, so when the cluster goes down, alerts stop firing at exactly the moment they're needed most\", \"Alert storm during cascading failure — 500 services fail simultaneously, generating 2,000 alerts in 60 seconds, overwhelming the on-call engineer who can't find the root cause in the noise\"], (5) Common mistakes: [\"Setting static alert thresholds without seasonal baselines — 'error rate > 1%' fires every Sunday at 2 AM during batch jobs, and after the 50th false alarm, the on-call engineer mutes the alert permanently\", \"Only monitoring infrastructure metrics (CPU, memory, disk) and not business metrics (orders/sec, payments/sec), so the on-call doesn't notice revenue has dropped 50% because all servers look healthy while the payment API returns empty responses\"], (6) Summary: [\"Monitor the monitoring — alerting infrastructure must be independent of the systems it monitors\", \"Alert grouping and root cause correlation reduce 500 alerts to 1 actionable notification during cascading failures\", \"Runbooks attached to every alert: who to page, what to check first, how to mitigate — reduces MTTR from hours to minutes\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/monitoring-alerting.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-355",
    "epic": "SDS",
    "title": "Rewrite Netflix Streaming teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Netflix Streaming at F. Add teachingContent section with: (1) Hook: \"Netflix accounts for 15% of all global internet traffic. During peak hours, 250 million subscribers stream simultaneously. One bottleneck in the video delivery pipeline means millions of spinning buffer wheels and a flood of subscription cancellations. How do you deliver 4K video to a quarter billion people without a single buffer?\", (2) Analogy: \"Like a chain of neighborhood libraries — instead of everyone driving to one central library downtown (origin server), your local branch (CDN edge server) stocks copies of the most popular books (video segments). Rare requests go to the regional branch, and only the rarest go to the central library.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Adaptive bitrate switching oscillation — a user on a fluctuating mobile connection rapidly alternates between 1080p and 480p every 5 seconds, creating a jarring visual experience worse than sticking at 480p consistently\", \"New content cold cache — a new show drops at midnight and 50 million users hit play simultaneously, but no edge server has the content cached yet, creating a thundering herd that overwhelms the origin server\"], (5) Common mistakes: [\"Transcoding to a fixed set of bitrates instead of optimizing per-title — an animated show looks perfect at 1.5 Mbps while a fast-action movie needs 8 Mbps for the same perceived quality, wasting bandwidth or quality by using one size fits all\", \"Using a single origin server region for video segments, creating a single point of failure and forcing all cache misses to traverse a continent instead of having regional origins\"], (6) Summary: [\"CDN hierarchy (edge -> regional -> origin) serves 95%+ of video bytes from edge, keeping origin load minimal\", \"Per-title encoding optimization: analyze content complexity to pick optimal bitrate ladder, saving 20-30% bandwidth at same quality\", \"Pre-warm edge caches before high-profile content launches — don't wait for the thundering herd to hit\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/netflix-cdn.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-356",
    "epic": "SDS",
    "title": "Rewrite Notification System teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Notification System at F. Add teachingContent section with: (1) Hook: \"Facebook sends 10 billion notifications per day across push, email, SMS, and in-app channels. A single poorly timed notification batch can cost $500K in SMS fees in minutes. And if users feel spammed, they disable notifications entirely — losing your most direct engagement channel forever. How do you build a system that's both high-throughput and respectful?\", (2) Analogy: \"Like a hotel concierge desk — guests leave their preferred contact method (push, email, SMS). When there's a message for a guest, the concierge tries their preferred channel first, escalates to the next if undelivered, and never knocks on the door at 3 AM unless it's an emergency.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Duplicate notification on retry — the push notification provider times out but actually delivered the message, so your retry sends it again, and the user sees the same notification twice\", \"Notification cascade — a viral post generates 100,000 comments in an hour, and the post author receives 100,000 individual comment notifications that crash their phone's notification tray\"], (5) Common mistakes: [\"Sending notifications synchronously in the request path (e.g., sending email during checkout), adding 2-5 seconds of latency and making the entire checkout flow fail if the email provider is down\", \"Not implementing per-user rate limiting and aggregation, sending 50 individual 'X liked your post' notifications instead of one '50 people liked your post' summary\"], (6) Summary: [\"Async queue-based delivery: decouple notification creation from delivery so provider failures don't affect the core product\", \"Idempotency keys per notification + provider: prevent duplicate delivery across retries\", \"Per-user frequency capping and aggregation: batch similar notifications and respect quiet hours to prevent notification fatigue\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/notification-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-357",
    "epic": "SDS",
    "title": "Rewrite Payment Gateway teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Payment Gateway at F. Add teachingContent section with: (1) Hook: \"Stripe processes 817 billion dollars annually. Every payment must be processed exactly once — charge a customer twice and you face a chargeback and legal liability. Fail to charge and you lose the sale. And the entire flow must complete in under 2 seconds across 5 external provider hops. How do you guarantee exactly-once payment execution in a distributed system where networks are unreliable?\", (2) Analogy: \"Like a certified mail system for money — you send the payment with a tracking number (idempotency key), get a receipt at every step (audit trail), and if the delivery fails midway, the tracking number ensures the money either arrives exactly once or is returned, never duplicated.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Timeout ambiguity — the payment processor doesn't respond within 5 seconds. Did it charge the card or not? You can't retry without risking a double charge, and you can't assume failure without risking a lost payment\", \"Currency conversion race — a user initiates payment in EUR, but by the time the charge reaches the processor 2 seconds later, the EUR/USD rate has changed, and the merchant receives less than quoted\"], (5) Common mistakes: [\"Not implementing idempotency keys for payment creation, so a network timeout followed by a client retry creates two charges for the same order — the most expensive bug in fintech\", \"Storing sensitive card data in application logs or databases instead of tokenizing through a PCI-compliant vault, creating a compliance violation that can result in fines up to $500K per incident\"], (6) Summary: [\"Idempotency keys are non-negotiable: every payment operation must be safely retryable without side effects\", \"Two-phase commit pattern: authorize first (reserve funds), capture second (actually charge) — decouple intent from execution\", \"Never store raw card numbers — tokenize through a PCI-compliant provider and use tokens for all subsequent operations\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/payment-gateway.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-358",
    "epic": "SDS",
    "title": "Rewrite Payment System teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Payment System at F. Add teachingContent section with: (1) Hook: \"PayPal handles 25 billion transactions per year across 200 countries and 25 currencies. A payment that's 'pending' for more than 10 seconds makes users refresh, retry, and potentially double-pay. But behind the scenes, a single payment touches fraud detection, currency conversion, bank APIs, and ledger reconciliation — all of which can fail independently. How do you make 6 unreliable steps feel like one atomic click?\", (2) Analogy: \"Like an escrow service for buying a house — the money doesn't go directly from buyer to seller. It's held by a trusted intermediary (escrow/ledger) who verifies all conditions are met before releasing funds, and if anything goes wrong, the transaction is reversed cleanly.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Partial failure in saga — fraud check passes, funds are reserved, but the merchant's account is frozen mid-transaction, requiring a compensating transaction to release the reserved funds back to the buyer\", \"Double-spend attempt — a user with $100 balance simultaneously initiates two $80 purchases from different devices, and both pass the balance check before either debit is applied\"], (5) Common mistakes: [\"Using distributed transactions (2PC) across microservices for every payment, creating tight coupling and a single point of failure at the coordinator — use sagas with compensating transactions for better resilience\", \"Reconciling ledger entries daily instead of in real-time, allowing discrepancies between the payment database and the bank to accumulate for hours before detection\"], (6) Summary: [\"Saga pattern with compensating transactions: each step has an explicit undo operation for graceful failure handling\", \"Double-entry bookkeeping: every debit has a matching credit, making the ledger self-auditing and discrepancies immediately detectable\", \"Serialize per-user operations to prevent double-spend: use distributed locks or sequential processing per user account\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/payment-system.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-359",
    "epic": "SDS",
    "title": "Rewrite Push Notification teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Push Notification at F. Add teachingContent section with: (1) Hook: \"WhatsApp delivers push notifications to 2 billion devices across iOS and Android within 500ms of a message being sent. But Apple's APNs and Google's FCM are external services with their own rate limits, error codes, and delivery semantics. A device token can become invalid at any moment. How do you reliably reach 2 billion devices through providers you don't control?\", (2) Analogy: \"Like a letter carrier service that only delivers to registered mailboxes — each device must register its mailbox address (token) with the service, and if a family moves (reinstalls the app), the old address becomes invalid. You need to track which addresses are still active and route each letter to the right carrier (APNs or FCM).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Token recycling — APNs invalidates a device token for User A's old phone and reassigns the same token to User B's new phone, so notifications meant for A start arriving on B's device if you don't process feedback promptly\", \"Silent push quota exceeded — iOS limits the number of silent/background pushes per hour, and if you exceed it, critical notifications get throttled with no error returned\"], (5) Common mistakes: [\"Sending push notifications one-by-one to the provider API instead of using batch APIs (FCM supports 500 per request, APNs uses HTTP/2 multiplexing), turning a 1-second batch operation into a 10-minute sequential crawl\", \"Not processing APNs/FCM feedback to remove invalid tokens, continuing to send to millions of dead tokens, wasting compute and eventually getting rate-limited by the provider\"], (6) Summary: [\"Maintain a clean token registry: process provider feedback to remove invalid tokens, handle token refresh events from clients\", \"Batch sends via provider APIs: FCM batch 500/request, APNs HTTP/2 multiplexing — never send one-by-one\", \"Platform-specific handling: iOS has payload size limits (4KB), background push quotas, and different priority semantics than Android\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/push-notification.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-360",
    "epic": "SDS",
    "title": "Rewrite Rate Limiter teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Rate Limiter at F. Add teachingContent section with: (1) Hook: \"Twitter's API was hit with 50,000 requests per second from a single botnet during the 2020 election. Without rate limiting, that botnet would have consumed all API capacity, making Twitter inaccessible for 400 million real users. How do you build a rate limiter that stops abuse without punishing legitimate users who occasionally burst?\", (2) Analogy: \"Like a water faucet with a flow restrictor — water (requests) flows freely up to a set rate, but turn the faucet beyond max and the restrictor kicks in. A token bucket adds a small reservoir so brief bursts are fine, but sustained over-use gets throttled.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Distributed rate limiting with eventual consistency — a user sends requests to two different data centers, and each sees only half the traffic, effectively allowing 2x the intended rate until the counters sync\", \"Rate limit by API key when the same key is shared across a microservice fleet — 100 instances each sending 10 req/s appear as 1000 req/s on one key, hitting limits that a single client never would\"], (5) Common mistakes: [\"Implementing a fixed-window counter and not realizing a user can send 2x the limit by timing bursts at the window boundary (100 requests at 11:59:59 and 100 more at 12:00:00 in a 100/min limit)\", \"Returning generic 429 errors without Retry-After headers or rate limit response headers (X-RateLimit-Remaining, X-RateLimit-Reset), forcing clients to guess when to retry and creating thundering herds\"], (6) Summary: [\"Token bucket allows controlled bursts while enforcing average rate; sliding window log prevents boundary abuse\", \"Always return rate limit headers (Retry-After, X-RateLimit-Remaining) so well-behaved clients self-throttle\", \"Layer rate limits: per-IP (anti-DDoS), per-user (fair usage), per-endpoint (protect expensive operations)\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/rate-limiter.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-361",
    "epic": "SDS",
    "title": "Rewrite Real-Time Chat Slack teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Real-Time Chat Slack at F. Add teachingContent section with: (1) Hook: \"Slack handles 1.5 billion messages per week across 750,000 organizations. A single workspace can have 100,000 users across 10,000 channels, and a message posted in #announcements must appear for every online member within 200ms — while also being instantly searchable. How do you build a chat system that's real-time, searchable, and handles the 'enterprise everything channel' problem?\", (2) Analogy: \"Like a corporate building's intercom system with recording — every announcement (message) goes out live on the right floor (channel), but it's also recorded (indexed) so anyone who missed it can search the transcript later. Each floor can have different access rules (permissions), and some floors have thousands of people while others have just two.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Large channel fan-out — a workspace-wide #general channel with 50,000 members generates 50,000 WebSocket pushes per message, but at 100 messages/minute, that's 5 million pushes/minute for one channel alone\", \"Presence at scale — tracking whether 100,000 users are online/away/offline and broadcasting status changes creates O(N^2) update volume as each user subscribes to hundreds of other users' presence\"], (5) Common mistakes: [\"Storing messages in a relational database with a simple B-tree index on (channel_id, timestamp) and expecting full-text search to work, instead of using a separate search index (Elasticsearch) that's updated asynchronously\", \"Opening a separate WebSocket connection per channel instead of one connection per user with server-side channel multiplexing, consuming 10,000x more connections than necessary\"], (6) Summary: [\"One WebSocket per user, server-side channel multiplexing — connection count scales with users, not users x channels\", \"Message store (Cassandra/Vitess) for ordered history + search index (Elasticsearch) for full-text — different access patterns need different stores\", \"Presence uses probabilistic protocols (heartbeat + timeout) and is intentionally eventually consistent — nobody needs exact real-time presence\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/real-time-chat-v2.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-362",
    "epic": "SDS",
    "title": "Rewrite Recommendation Engine teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Recommendation Engine at F. Add teachingContent section with: (1) Hook: \"Netflix estimates its recommendation engine saves $1 billion per year in reduced churn. 80% of content watched on Netflix is discovered through recommendations, not search. But with 15,000 titles and 250 million users, computing personalized recommendations for every user in real-time is computationally infeasible. How do you serve personalized recommendations in under 100ms when the candidate space is enormous?\", (2) Analogy: \"Like a personal shopper at a department store — they know your style (user profile), what similar customers bought (collaborative filtering), and what's trending this season (content features). They pre-select a rack of options (candidate generation) and then pick the top 5 for you (ranking).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Cold start problem — a new user with zero interaction history joins, and collaborative filtering has nothing to work with, requiring fallback to content-based or popularity-based recommendations that feel generic\", \"Popularity bias feedback loop — the system recommends popular items, users click on them (confirming the recommendation), which makes them more popular, suppressing niche content that might actually be better for the user\"], (5) Common mistakes: [\"Computing recommendations in real-time for every request instead of pre-computing candidate sets offline and only re-ranking in real-time, making p99 latency spike to seconds during traffic peaks\", \"Evaluating recommendation quality only by click-through rate, ignoring long-term metrics like user retention and diversity — optimizing purely for clicks leads to clickbait recommendations that degrade trust\"], (6) Summary: [\"Two-stage architecture: offline candidate generation (cheap, covers millions) + online ranking (expensive, picks top-N from hundreds)\", \"Blend collaborative filtering (what similar users liked), content-based (what's similar to what you liked), and popularity — each covers the other's blind spots\", \"Measure diversity and novelty alongside engagement — recommendation monocultures destroy long-term user retention\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/recommendation-engine.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-363",
    "epic": "SDS",
    "title": "Rewrite Ride Sharing teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Ride Sharing at F. Add teachingContent section with: (1) Hook: \"Uber completes 25 million rides per day across 10,000 cities. At peak hour, 500,000 riders request a ride simultaneously, each expecting a driver match within 15 seconds. The matching algorithm must consider driver location, traffic, rider destination, surge pricing, and driver preferences — all computed in under a second. How do you build a real-time marketplace that matches supply and demand across an entire city?\", (2) Analogy: \"Like a massive speed-dating event — hundreds of thousands of people (riders) and hosts (drivers) arrive simultaneously, and the organizer (dispatch) must pair them based on proximity, preferences, and availability in under 15 seconds, while the venue (city) keeps changing shape due to traffic.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Driver goes offline mid-dispatch — the system assigns a driver who closes the app before accepting, wasting 30 seconds of the rider's time while the algorithm must re-match with a potentially farther driver\", \"GPS drift in urban canyons — downtown buildings cause GPS to show the driver on the wrong street, and the rider and driver can't find each other even though the app says they're 10 meters apart\"], (5) Common mistakes: [\"Using a simple nearest-driver algorithm instead of a supply-demand balancing approach, causing all drivers to cluster near high-demand areas while suburban riders wait 20+ minutes\", \"Recomputing the full driver supply index from scratch every second instead of maintaining a real-time geospatial index with incremental updates, making matching latency scale linearly with fleet size\"], (6) Summary: [\"Geospatial indexing (H3/S2 hexagonal cells) with real-time driver location streaming enables O(1) nearest-driver queries\", \"Dispatch optimization considers ETA, driver direction, demand prediction, and fairness — not just shortest distance\", \"Surge pricing is a supply-demand signal: it simultaneously incentivizes more drivers to come online and reduces rider demand, equilibrating the marketplace\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/ride-sharing.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-364",
    "epic": "SDS",
    "title": "Rewrite Search Autocomplete teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Search Autocomplete at F. Add teachingContent section with: (1) Hook: \"Google's autocomplete serves suggestions within 50ms of every keystroke for 8.5 billion daily searches. At 5 characters per second average typing speed, that's 42 billion autocomplete requests per day — each needing to search through billions of possible completions and return the top 10 before the next key is pressed. How do you make search feel instantaneous?\", (2) Analogy: \"Like a librarian who starts pulling books off the shelf before you finish saying the title — after hearing 'Harry Po...', they're already reaching for Harry Potter because they know from experience what most people are asking for.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Prefix ambiguity explosion — the prefix 'a' matches 500 million possible completions, and computing the top 10 by popularity from that set in real-time is infeasible without pre-computed prefix aggregations\", \"Offensive suggestion filtering — autocomplete must not suggest harmful, defamatory, or illegal queries, even if they're popular searches, requiring a real-time blocklist check on every suggestion set\"], (5) Common mistakes: [\"Querying a database with LIKE 'prefix%' on every keystroke instead of using a pre-built trie or prefix hash map, turning a 1ms lookup into a 200ms full-index scan\", \"Not debouncing client-side keystrokes, sending a request on every single keypress and wasting 60-80% of requests on prefixes the user typed through without pausing to read suggestions\"], (6) Summary: [\"Trie data structure (or prefix hash map) with pre-aggregated top-K results per prefix node enables O(prefix length) lookups\", \"Client-side debounce (100-200ms) + request cancellation eliminates 70% of wasted requests\", \"Separate fast-updating 'trending' layer from slow-updating 'historical' completions to surface fresh suggestions without rebuilding the entire index\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/search-autocomplete.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-365",
    "epic": "SDS",
    "title": "Rewrite Search Engine teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Search Engine at F. Add teachingContent section with: (1) Hook: \"Elasticsearch powers search for GitHub (200M+ repos), Wikipedia (60M articles), and Netflix (15K titles). A user types a query, hits enter, and expects relevant results ranked perfectly in under 100ms — even when the query is misspelled and the index has 10 billion documents. How do you build a search engine that understands what users mean, not just what they type?\", (2) Analogy: \"Like the index at the back of a textbook, but inverted — instead of topic -> page numbers (forward index), it's word -> list of every document containing that word (inverted index). Finding documents with both 'distributed' and 'cache' means intersecting two sorted lists, which is blazingly fast.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Relevance vs recency trade-off — a query for 'best JavaScript framework' should prioritize recent articles (2024) over a highly-cited 2018 article about jQuery, but pure recency ranking surfaces low-quality new content\", \"Synonyms and stemming gone wrong — searching for 'running shoes' returns results about 'running a business' because the stemmer reduces both to 'run', degrading precision\"], (5) Common mistakes: [\"Rebuilding the entire index on every document update instead of using incremental indexing with merge-on-read, causing index freshness to lag by hours instead of seconds\", \"Using a single relevance score (BM25) without incorporating click-through signals, freshness boost, and field weights — resulting in technically relevant but practically useless results ranked above useful ones\"], (6) Summary: [\"Inverted index + TF-IDF/BM25 scoring handles keyword search; add vector embeddings for semantic 'meaning' search\", \"Shard the index by document ID and scatter-gather across shards — search latency stays constant as index grows\", \"Multi-signal ranking (text relevance + freshness + popularity + personalization) is where search quality is won or lost\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/search-engine.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-366",
    "epic": "SDS",
    "title": "Rewrite Social Media Feed teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Social Media Feed at F. Add teachingContent section with: (1) Hook: \"Facebook's News Feed serves 2 billion users, each seeing a personalized stream drawn from an average of 1,500 candidate posts per session. Generating that feed means evaluating 1,500 posts against a ranking model with 100+ features — for every single user, on every single app open. Doing this in real-time for 2 billion people is computationally absurd. So how does Facebook make it feel instant?\", (2) Analogy: \"Like a personal newspaper editor — every morning, your editor (ranking model) reviews 1,500 articles (posts from friends/pages), picks the top 30, arranges them by what you're most likely to enjoy, and delivers the paper to your doorstep (feed). The editor learns your preferences over time and adjusts.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Feed ranking becomes an echo chamber — the model learns the user only engages with political content, so it shows more political content, reducing exposure to diverse topics and creating a filter bubble\", \"Post deletion propagation — a user deletes an embarrassing post, but it's already been fan-out-on-write to 5,000 followers' feed caches, and invalidating all those cached feeds instantly is expensive\"], (5) Common mistakes: [\"Sorting feed purely by chronological order (newest first) instead of by relevance, causing users to see low-quality recent posts over high-quality posts from 2 hours ago — engagement drops 20-40% with chronological feeds at scale\", \"Materializing the entire feed in a database table per user (pure fan-out on write), so a user with 10,000 followers generates 10,000 write operations per post, crushing write throughput\"], (6) Summary: [\"Hybrid fan-out: pre-compute feeds for active users (push), compute on-demand for inactive users (pull) — don't waste writes on users who check once a week\", \"Two-pass ranking: light candidate selection (hundreds) then heavy ML re-ranking (top 50) — keeps latency under 200ms\", \"Cache the ranked feed result, not individual posts — invalidation is per-feed, not per-post\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/social-feed.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-367",
    "epic": "SDS",
    "title": "Rewrite Social Login OAuth teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Social Login OAuth at F. Add teachingContent section with: (1) Hook: \"77% of apps offer 'Sign in with Google' because it reduces signup friction by 50%. But OAuth is a delegation protocol, not an authentication protocol — and confusing the two has led to account takeover vulnerabilities in apps used by millions. How do you implement social login securely when the spec itself doesn't guarantee identity?\", (2) Analogy: \"Like using your driver's license (Google account) to verify your age at a bar (third-party app) — the bar sees enough to let you in (identity assertion) without seeing your home address or Social Security number (credentials). The DMV (Google) vouches for you without revealing everything.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Account linking collision — a user first signs up with email/password, then later clicks 'Sign in with Google' using the same email address, creating a duplicate account instead of linking to the existing one\", \"Provider revocation — a user revokes your app's access in their Google account settings, but your app still has a valid refresh token cached and continues accessing their data until it expires\"], (5) Common mistakes: [\"Using OAuth 2.0 access tokens as proof of identity instead of using OpenID Connect (OIDC) ID tokens, which are signed JWTs containing the user's identity — an access token proves authorization, not authentication\", \"Storing the OAuth state parameter in a predictable way or not using it at all, enabling CSRF attacks where an attacker can link their social account to the victim's application account\"], (6) Summary: [\"Use OpenID Connect (OIDC), not raw OAuth 2.0 — OIDC adds the identity layer (ID token) that OAuth deliberately omits\", \"Always use the state parameter with a cryptographic nonce to prevent CSRF during the OAuth flow\", \"Implement account linking: when social login email matches an existing account, prompt verification before merging — never auto-merge\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/social-login.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-368",
    "epic": "SDS",
    "title": "Rewrite Spotify Streaming teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Spotify Streaming at F. Add teachingContent section with: (1) Hook: \"Spotify has 600 million users streaming 100 million tracks. When you press play, audio must start within 200ms — not after buffering. And when the next song in your playlist starts, the transition must be gapless. Behind the scenes, Spotify pre-fetches the next track, negotiates audio quality based on your connection, and manages 4 billion playlist-track relationships. How do you make infinite music feel instantaneous?\", (2) Analogy: \"Like a DJ with two turntables — while one song is playing (current track streaming), the DJ is already cueing up the next record on the other turntable (pre-fetching). The crossfade between them is seamless, and the DJ adjusts the audio quality based on the venue's sound system (adaptive bitrate).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Gapless playback across encoding boundaries — two consecutive tracks encoded at different bitrates create an audible gap or pop at the transition if the decoder buffer isn't drained precisely before switching codecs\", \"Offline sync conflict — a user downloads a playlist offline, then the artist removes a track from the platform, and on next sync the app must remove the track from the device while preserving the rest of the playlist\"], (5) Common mistakes: [\"Streaming the entire file sequentially instead of using chunked delivery with byte-range requests, preventing seek operations (jumping to the middle of a song) from working without downloading everything before the seek point\", \"Storing playlist-track relationships in a relational table with row-per-track and reordering by updating sort indices, causing O(N) updates for every reorder operation — use a linked-list or fractional indexing approach instead\"], (6) Summary: [\"Chunked audio delivery with byte-range requests enables instant seek and adaptive bitrate switching mid-stream\", \"Pre-fetch the next track during current playback + client-side buffer management = gapless transitions\", \"Playlist storage at scale: fractional indexing for O(1) reordering, denormalized for read-heavy access patterns\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/spotify.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-369",
    "epic": "SDS",
    "title": "Rewrite Stock Trading teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Stock Trading at F. Add teachingContent section with: (1) Hook: \"The NYSE processes 6 billion messages per day with a matching engine latency of 20 microseconds. A single microsecond advantage is worth $100M/year to high-frequency traders. But beyond raw speed, the system must guarantee that no order is ever lost, no trade is executed at an incorrect price, and every transaction is auditable by regulators. How do you build a system that's simultaneously the fastest and most reliable on Earth?\", (2) Analogy: \"Like an auction house on hyperdrive — buyers shout bids, sellers shout asks, and the auctioneer (matching engine) pairs them in strict price-time priority. But unlike a physical auction, this one processes 100,000 items per second, and every match is recorded in a legally binding ledger.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Split-brain in matching engine failover — the primary matching engine fails and the secondary takes over, but during the transition, both briefly accept orders and produce different match results, requiring reconciliation without any trader losing money\", \"Order book flash crash — a large market sell order depletes all buy orders in the book, causing the price to collapse to near zero in milliseconds before circuit breakers trigger\"], (5) Common mistakes: [\"Using a general-purpose message queue (Kafka) for the order matching path instead of a purpose-built single-threaded sequential processor, adding milliseconds of latency that make the system unusable for professional trading\", \"Implementing matching as price-only without time priority (FIFO at each price level), allowing later orders to jump the queue and creating an unfair market that regulators will flag\"], (6) Summary: [\"Single-threaded matching engine with event sourcing: all orders are sequenced in a deterministic log, enabling exact replay for audit and failover\", \"Price-time priority (FIFO per price level) is the standard fairness model — implement it correctly or face regulatory action\", \"Circuit breakers (trading halts at 5%/10%/20% drops) prevent flash crashes from cascading — build them into the engine, not as an afterthought\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/stock-exchange.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-370",
    "epic": "SDS",
    "title": "Rewrite Task Queue teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Task Queue at F. Add teachingContent section with: (1) Hook: \"Shopify's task queue processes 10 billion background jobs per day — from sending order confirmation emails to generating PDF invoices to resizing product images. If the queue goes down, the entire e-commerce platform grinds to a halt because every async operation depends on it. And a single poison message (a job that always fails) can block the entire queue. How do you build a queue that's both reliable and resilient?\", (2) Analogy: \"Like a restaurant kitchen's order ticket system — servers (producers) clip tickets to a rail, and cooks (workers) pull tickets in order. If a cook can't make a dish (job fails), the ticket goes to a separate 'remake' rail (retry queue) instead of blocking all the tickets behind it.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Poison message — a job with malformed input always throws an exception, gets retried infinitely, and blocks the queue for all subsequent jobs. Without a dead letter queue (DLQ) and max retry limit, one bad job halts the entire system\", \"At-least-once delivery causing duplicate processing — a worker processes a job, but the acknowledgment fails due to network timeout, so the queue re-delivers the job to another worker, executing the same job twice\"], (5) Common mistakes: [\"Processing jobs in a single FIFO queue without priority separation, so a flood of low-priority image resizing jobs delays high-priority payment confirmation jobs by minutes\", \"Not implementing job idempotency, so the inherent at-least-once delivery semantics of message queues cause duplicate emails, duplicate charges, or duplicate API calls on every retry\"], (6) Summary: [\"Dead letter queues (DLQ) with max retry limits isolate poison messages so they don't block healthy jobs\", \"Every job must be idempotent — queues guarantee at-least-once delivery, so your worker must handle duplicates safely\", \"Priority queues separate critical jobs (payments) from bulk jobs (image resizing) so latency-sensitive work isn't blocked\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/task-queue.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-371",
    "epic": "SDS",
    "title": "Rewrite Event Ticket Booking teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Event Ticket Booking at F. Add teachingContent section with: (1) Hook: \"When Taylor Swift's Eras Tour tickets went on sale, Ticketmaster received 3.5 billion requests in a single day — 14 million users competing for 2 million seats. The site crashed. Users queued for hours. Some bought tickets that were already sold to someone else. How do you build a booking system that can handle billion-request spikes without overselling a single seat?\", (2) Analogy: \"Like a deli counter with numbered tickets — everyone takes a number (virtual queue), and you're served in order when your turn comes. The counter display (inventory) shows exactly how many sandwiches (seats) are left, and the moment you're served, that sandwich is removed from the display before the next customer is called.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Seat hold expiration race — User A holds seat R12-S5 for 10 minutes during checkout. At minute 9:59, User B sees the seat become available and clicks it, but User A completes payment at 10:00. Both users believe they own the same seat\", \"Bot scalping — bots complete the checkout flow 100x faster than humans, buying thousands of tickets before any real fan reaches the payment page, despite CAPTCHA protections\"], (5) Common mistakes: [\"Using a virtual waiting room that gives users a position number but doesn't actually process requests in order, allowing savvy users to open multiple browser tabs and bypass the queue\", \"Implementing seat selection as a two-step process (select, then reserve) without holding the seat during selection, so 100 users all select the same 'available' seat and 99 get an error at the reserve step\"], (6) Summary: [\"Virtual queue + admission rate control: let users in at a controlled rate that matches your system's booking throughput\", \"Atomic seat reservation: hold the seat the moment it's selected (with TTL), not when payment starts — eliminates the select-then-fail pattern\", \"Inventory counter in Redis with atomic decrement: overselling is prevented at the counter level, not by database locks on individual seats\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/ticket-booking.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-372",
    "epic": "SDS",
    "title": "Rewrite Twitter/X Timeline teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Twitter/X Timeline at F. Add teachingContent section with: (1) Hook: \"When a tweet goes viral, it needs to appear in the timelines of potentially 100 million followers. Lady Gaga has 85 million followers — writing her tweet into 85 million individual timelines would take minutes and generate terabytes of write I/O. But showing a chronological timeline is useless without fan-out. How did Twitter solve the fan-out problem that almost killed them in 2012?\", (2) Analogy: \"Like a newspaper with two distribution models — for columnists with 100 readers (small accounts), you print and deliver a copy to each reader's doorstep (fan-out on write). For front-page news seen by millions (celebrity tweets), you post it on a bulletin board and readers check it when they walk by (fan-out on read).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Mixed timeline merge — a user follows both celebrities (fan-out on read) and friends (fan-out on write), and their timeline must merge both sources seamlessly, sorted by relevance or time, without duplicate tweets\", \"Delete propagation — a user deletes a tweet that was already fanned-out to 50 million timelines. Removing it from every individual cache/timeline is expensive, so the system must handle 'tombstone' markers that hide deleted tweets at read time\"], (5) Common mistakes: [\"Using pure fan-out on write for all users, so a single celebrity tweet triggers 100M write operations that overwhelm the timeline service and delay tweet delivery for everyone\", \"Not pre-computing timeline caches for active users, forcing an expensive real-time aggregation across hundreds of followed accounts on every timeline refresh, adding seconds of latency\"], (6) Summary: [\"Hybrid fan-out: push tweets for accounts with <5K followers, pull at read time for celebrities — merge both at timeline read\", \"Timeline cache per active user (Redis list of tweet IDs) makes reads O(1) — only store IDs, fetch full tweets from a separate tweet store\", \"Tombstone markers for deleted tweets: cheaper than propagating deletes to all fan-out caches immediately\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/twitter-fanout.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-373",
    "epic": "SDS",
    "title": "Rewrite Typeahead teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Typeahead at F. Add teachingContent section with: (1) Hook: \"LinkedIn's search typeahead suggests names, companies, and job titles from a corpus of 1 billion entities. After typing just 2 characters, the user expects 5 relevant suggestions in under 50ms — faster than their next keystroke. And the suggestions must be personalized: 'Jo' should suggest 'John from your team' before 'Joe Biden' if you work at the same company. How do you personalize prefix matching across a billion entities in 50ms?\", (2) Analogy: \"Like a phone's predictive text that knows you — it doesn't just suggest common words, it prioritizes people in your contacts, apps you use often, and places you've been. The trie holds the entire dictionary, but your personal usage weights which branches to explore first.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Unicode and transliteration — a user types 'Muller' but the entity is stored as 'Muller', or types in Pinyin while the entity name is in Chinese characters, requiring cross-script prefix matching\", \"Index staleness — a user changes their name or a company is renamed, but the typeahead index (pre-built trie/FST) still returns the old name until the next index rebuild, showing stale suggestions\"], (5) Common mistakes: [\"Building a single global trie for all users without any personalization layer, so typing 'Ma' returns 'Mark Zuckerberg' before 'Maria from your team' even though Maria is in 5 of your recent conversations\", \"Returning the same suggestions regardless of the context (search box, messaging, job search), when each context has different intent and should weight entities differently\"], (6) Summary: [\"Pre-built trie/FST with top-K per prefix node: global suggestions in O(prefix length), not O(corpus size)\", \"Personalization overlay: boost entities from user's network, recent interactions, and current context on top of global suggestions\", \"Two-tier architecture: in-memory trie for top 10M entities + on-disk fallback for the long tail — cache hot paths aggressively\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/typeahead.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-374",
    "epic": "SDS",
    "title": "Rewrite Uber Ride Dispatch teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Uber Ride Dispatch at F. Add teachingContent section with: (1) Hook: \"Uber's dispatch system matches a rider to a driver in under 10 seconds, evaluating hundreds of candidate drivers across a city grid of thousands of hexagonal cells. During New Year's Eve, request volume spikes 3x in 30 minutes. The system must continue matching in real-time while dynamically adjusting surge pricing — a few seconds of dispatch downtime means thousands of stranded riders. How do you build the brain that runs a city's transportation in real-time?\", (2) Analogy: \"Like an air traffic control tower — dozens of planes (drivers) are in the air at any moment, each with a specific position and heading (location and direction). When a new flight (ride request) needs a gate (pickup), the controller assigns the optimal plane considering position, fuel (driver availability), and runway congestion (traffic).\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Supply relocation — after a major event ends, all drivers are in one area (stadium) and all demand is in another area (suburbs), creating a geographic mismatch that takes 20+ minutes to naturally resolve without proactive repositioning incentives\", \"Dispatch race condition — two nearby riders request rides 100ms apart, both get matched to the same nearest driver because the first match hasn't propagated to the supply index yet\"], (5) Common mistakes: [\"Using Euclidean (straight-line) distance for driver matching instead of road-network ETA, assigning a driver who is 0.5km away as the crow flies but 15 minutes away because of a river or highway with no exit\", \"Running dispatch as a synchronous request-response API where the rider waits for a match, instead of an async event-driven system where the request enters a matching pool and the rider is notified when matched — sync dispatch can't handle batch optimization\"], (6) Summary: [\"Geospatial supply index (H3 cells + R-tree) with sub-second updates enables nearest-available-driver queries across millions of candidates\", \"Batch dispatch optimization: match multiple riders and drivers simultaneously for globally optimal assignments, not greedy one-at-a-time\", \"Surge pricing = supply/demand signal: real-time computation per H3 cell based on request:driver ratio, updated every 30 seconds\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/uber-dispatch.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-375",
    "epic": "SDS",
    "title": "Rewrite URL Shortener with Analytics teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored URL Shortener with Analytics at F. Add teachingContent section with: (1) Hook: \"Bitly tracks 10 billion clicks per month with real-time analytics — showing marketers which countries, devices, and times of day generate the most engagement, updating dashboards within seconds of each click. The redirect itself must complete in under 10ms, but the analytics pipeline behind it processes 4,000 events per second. How do you serve instant redirects while simultaneously feeding a real-time analytics engine?\", (2) Analogy: \"Like a turnstile at a subway station — it lets you through instantly (redirect), but silently counts every passage and records the time, direction, and card type (analytics metadata). The counting never slows down the entry, but the transit authority can see real-time ridership dashboards.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Analytics write amplification from bot traffic — a single shortened link shared on Twitter gets crawled by 200+ bot user agents (Googlebot, Twitterbot, Slackbot), inflating click counts 10x and polluting geographic/device analytics\", \"Dashboard aggregation lag during traffic spike — a viral link gets 1 million clicks in 10 minutes, but the real-time aggregation pipeline falls behind, showing a dashboard count of 200K while the redirect service has already served 1M\"], (5) Common mistakes: [\"Writing analytics events synchronously to a database in the redirect path, adding 5-20ms of latency to every redirect and making the analytics database a single point of failure for the core redirect functionality\", \"Storing raw click events forever without pre-aggregating, so a link with 100M total clicks requires scanning 100M rows to render a simple 'total clicks' dashboard widget\"], (6) Summary: [\"Fire-and-forget to Kafka on redirect (async): analytics must never add latency to the redirect path\", \"Pre-aggregate clicks into time buckets (minute/hour/day) during stream processing — never query raw events for dashboards\", \"Filter bot traffic using User-Agent classification and IP reputation before aggregation to keep analytics accurate\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/url-analytics.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-376",
    "epic": "SDS",
    "title": "Rewrite URL Shortener teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored URL Shortener at F. Add teachingContent section with: (1) Hook: \"bit.ly handles 500 million clicks per day. Every shortened link must resolve in under 10ms across 6 continents. A single hash collision means two different long URLs map to the same short URL, corrupting redirects for both. How do you generate billions of unique, short, collision-free identifiers while serving reads 100x faster than writes?\", (2) Analogy: \"Like a library card catalog — each card (short URL) maps to a shelf location (long URL), and the catalog must handle millions of lookups per second without slowing down. Adding a new card is rare compared to looking one up, so the catalog is optimized for fast retrieval.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Key exhaustion — with 7-character base62 keys, you have 3.5 trillion possible URLs. But if your key generation uses sequential IDs, an attacker can enumerate all shortened URLs by incrementing the ID, exposing private links\", \"Redirect loop — a user shortens a URL that itself is a shortened URL, creating a chain of redirects that browsers may refuse to follow after 10 hops\"], (5) Common mistakes: [\"Truncating MD5/SHA hashes to generate short keys, causing collisions after approximately 1 billion URLs due to the birthday problem — use a counter-based approach or pre-generated key pool instead\", \"Forgetting the 100:1 read-to-write ratio and under-provisioning the cache layer — a URL shortener is a read-heavy system where 99%+ of traffic is redirect lookups that should hit cache, not the database\"], (6) Summary: [\"Base62 encoding of a counter or pre-generated key pool avoids hash collisions entirely\", \"Read-heavy (100:1 ratio): cache the short->long mapping aggressively — 99% of redirects should never touch the database\", \"301 (permanent) vs 302 (temporary) redirect affects cacheability and analytics — 302 forces every click through your server for counting\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/url-shortener.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-377",
    "epic": "SDS",
    "title": "Rewrite Video Processing teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Video Processing at F. Add teachingContent section with: (1) Hook: \"YouTube receives 500 hours of video every minute. Each video must be transcoded into 20+ formats (resolution x codec x bitrate) before it's watchable. A 10-minute 4K upload generates 2TB of intermediate data during processing. If your pipeline falls behind by even 1 hour, you've accumulated 30,000 hours of unprocessed video. How do you build a processing pipeline that never falls behind?\", (2) Analogy: \"Like a car wash with multiple lanes and stages — cars (videos) enter one at a time, get split into parallel lanes for washing, waxing, and drying (different transcode jobs), and reassembled at the exit. If one lane is slow, you add more capacity to that specific lane without redesigning the whole wash.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Transcode job failure at 95% completion — a 2-hour video finishes 95% of its transcode and the worker crashes, losing all progress because no intermediate checkpointing was implemented\", \"Variable-length content — a 10-second clip and a 12-hour livestream recording both enter the same pipeline, but the 12-hour video monopolizes a worker for hours while short clips queue behind it\"], (5) Common mistakes: [\"Processing an entire video as a single job on one machine instead of splitting into segments (GOP-aligned chunks) and processing in parallel, making a 1-hour video take 1 hour to transcode instead of 5 minutes across 12 workers\", \"Transcoding all videos to all formats eagerly on upload, including rare combinations like 240p VP9 that may never be requested, wasting 50% of compute on variants nobody watches\"], (6) Summary: [\"Split videos into GOP-aligned segments, transcode segments in parallel, concatenate output — linear speedup with worker count\", \"Eager transcoding for popular formats (720p/1080p H.264), lazy transcoding for long-tail formats — don't waste compute on variants nobody requests\", \"Checkpointing per segment: if a worker crashes, only that segment needs reprocessing, not the entire video\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/video-processing.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-378",
    "epic": "SDS",
    "title": "Rewrite Web Crawler teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Web Crawler at F. Add teachingContent section with: (1) Hook: \"Googlebot crawls 100 billion pages. The web adds 4 million new pages per day. But you can't just blast every server with requests — aggressive crawling gets you IP-banned, and polite crawling means it takes years to index the web. How do you crawl the entire internet efficiently while being a good citizen that doesn't accidentally DDoS small websites?\", (2) Analogy: \"Like a team of door-to-door census workers — each worker (crawler thread) visits houses (web pages) on their assigned street (domain). They have a schedule that ensures they don't knock on the same door twice, they respect 'do not disturb' signs (robots.txt), and they report back to headquarters (URL frontier) about new streets they discovered.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Spider trap — a website dynamically generates infinite URLs (e.g., a calendar page with next/prev links going to year 9999), trapping the crawler in an infinite loop that consumes all capacity on one domain\", \"Soft 404 — a page returns HTTP 200 but the body says 'Page not found.' The crawler indexes it as a valid page, polluting the index with thousands of near-identical error pages\"], (5) Common mistakes: [\"Not implementing per-domain rate limiting (politeness policy), so the crawler sends 1,000 requests/second to a small blog server, effectively DDoS-ing it and getting your entire IP range blocked\", \"Using a simple visited-URL set in memory that grows unboundedly — after crawling 10 billion URLs, the set consumes 1TB of RAM. Use a Bloom filter (probabilistic, fixed memory) for URL deduplication\"], (6) Summary: [\"URL frontier with per-domain queues and politeness delays ensures you crawl broadly without overwhelming any single site\", \"Bloom filter for URL deduplication: O(1) lookup, fixed memory, trades a tiny false-positive rate for 1000x memory savings\", \"Prioritize recrawling high-value, frequently-changing pages (news sites) over static pages (documentation) — not all pages need the same freshness\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/web-crawler.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-379",
    "epic": "SDS",
    "title": "Rewrite Workflow Orchestrator teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored Workflow Orchestrator at F. Add teachingContent section with: (1) Hook: \"Uber's workflow engine orchestrates 100,000 concurrent workflows for trip processing, payment settlement, and driver onboarding. A single trip involves 15 steps across 8 services — any of which can fail, timeout, or need human approval. If the orchestrator loses track of a workflow mid-execution, a driver doesn't get paid or a rider gets charged twice. How do you build a system that reliably drives multi-step processes to completion, even across failures?\", (2) Analogy: \"Like a project manager with a checklist — each task (step) has a responsible team (service), a deadline (timeout), and a fallback plan (compensation). The project manager (orchestrator) tracks every task's status and, if a team fails, either retries, escalates, or triggers the rollback plan for all completed steps.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Long-running workflow hibernation — a workflow reaches a 'wait for human approval' step and needs to sleep for days or weeks, but keeping a thread/process alive that long wastes resources and risks losing state on restarts\", \"Workflow version migration — you need to update the workflow definition (add a new step), but 50,000 workflows are mid-execution on the old version, and you can't just restart them from scratch\"], (5) Common mistakes: [\"Using an in-memory state machine for workflow state, so a server restart loses all in-flight workflow progress — use a durable execution framework (Temporal, Step Functions) that persists state after every step\", \"Implementing choreography (services react to events) when you need orchestration (central coordinator), making it impossible to see the full workflow state, debug failures, or add timeouts to individual steps\"], (6) Summary: [\"Durable execution: persist workflow state after every step so crashes resume from the last checkpoint, never from scratch\", \"Saga pattern for distributed transactions: each forward step has a compensating backward step for clean rollback\", \"Workflow versioning: use deterministic replay (Temporal's model) so in-flight workflows continue on old logic while new workflows use updated definitions\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/workflow-engine.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  },
  {
    "id": "SDS-380",
    "epic": "SDS",
    "title": "Rewrite YouTube Video Platform teachingContent — hook, intuition, mistakes, summary",
    "description": "Teaching audit scored YouTube Video Platform at F. Add teachingContent section with: (1) Hook: \"YouTube serves 1 billion hours of video per day to 2.7 billion monthly users. Every minute, 500 hours of new video are uploaded. A single viral video can go from 0 to 100 million views in 24 hours, requiring the CDN to dynamically scale from zero cached copies to thousands of edge replicas. How do you build a platform that handles both the world's largest content library and the world's most unpredictable traffic patterns?\", (2) Analogy: \"Like a global chain of movie theaters — headquarters (origin) stores every film ever made, regional distributors (mid-tier CDN) stock popular titles, and local theaters (edge servers) show what their audience is watching. A surprise blockbuster quickly gets more copies printed and distributed, while obscure films are fetched on demand from headquarters.\", (3) Per-step WHY: add 'why this approach' to each learnStep, (4) Edge cases: [\"Live stream with millions of concurrent viewers — unlike pre-recorded video where segments are cached, a live stream generates segments in real-time that must be pushed to edge servers across the globe within 2-5 seconds of being created\", \"Copyright detection at upload — Content ID must scan every uploaded video against 100 million reference files in near-real-time, fingerprinting audio and video to catch matches even when the content is cropped, sped up, or pitch-shifted\"], (5) Common mistakes: [\"Serving video through a single CDN provider instead of multi-CDN with real-time quality-of-experience switching, so a CDN outage in one region causes buffering for millions of users instead of automatically routing to an alternative\", \"Using a single monolithic video processing pipeline for both short clips (< 1 min) and long-form content (3+ hours), when short clips need fast turnaround (< 30 seconds) and long videos need parallel segment processing — different SLAs require different pipelines\"], (6) Summary: [\"Three-tier CDN (edge, regional, origin) with popularity-based caching: top 1% of videos serve 80% of traffic from edge\", \"Adaptive bitrate streaming (DASH/HLS): client-driven quality switching based on bandwidth eliminates buffering\", \"Separate upload/processing pipelines by content type: short-form needs speed (30s processing), long-form needs parallel segmented transcoding\"].",
    "acceptanceCriteria": [
      "Hook opens with a problem/scale scenario, not a definition",
      "Analogy uses a real-world metaphor a non-engineer would understand",
      "Each learnStep has 1-2 sentences explaining WHY this approach over alternatives",
      "2 edge cases documented with what happens when they trigger",
      "2 common mistakes listed with explanation of why they fail",
      "3-bullet summary suitable for flashcard export"
    ],
    "priority": "P1",
    "effort": "S",
    "status": "ready",
    "phase": 2,
    "module": "system-design",
    "category": "frontend",
    "files": ["templates/system-design/youtube.json"],
    "dependencies": [],
    "blockedBy": ["SDS-236"],
    "tags": ["teaching", "rewrite", "per-template", "content"]
  }
]