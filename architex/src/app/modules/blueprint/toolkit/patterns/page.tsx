import { BlueprintComingSoon } from "@/components/modules/blueprint/BlueprintComingSoon";

export default function PatternsLibraryPage() {
  return (
    <BlueprintComingSoon
      subprojectId="SP4"
      hint="Patterns Library — browse all 36 patterns with inline canvas — ships in sub-project 4."
    />
  );
}
