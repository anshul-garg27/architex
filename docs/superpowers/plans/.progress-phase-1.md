# Phase 1 Progress Tracker

- [x] Phase 0 pre-flight audit complete (bug fixes already cherry-picked onto main)
- [x] Task 1: lld_drill_attempts schema
- [x] Task 2: migration generated and applied
- [x] Task 3: ui-store lldMode slice
- [x] Task 4: interview-store activeDrill slice
- [x] Task 5: useLLDModeSync hook
- [x] Task 6: useLLDPreferencesSync hook
- [x] Task 7: useLLDDrillSync hook
- [x] Task 8: GET /api/user-preferences
- [x] Task 9: PATCH /api/user-preferences/lld
- [x] Task 10: /api/lld/drill-attempts routes
- [x] Task 11: lld-events.ts analytics catalog
- [x] Task 12: ModeSwitcher component
- [x] Task 13: WelcomeBanner component
- [x] Task 14: 4 mode layout components
- [x] Task 15: LLDShell shell
- [x] Task 16: wire into useLLDModuleImpl
- [x] Task 17: smoke test + verification

Phase 1 complete on: 2026-04-20
Ready to start Phase 2: Learn mode + Wave 1 content (5 patterns).

## Verification snapshot

- typecheck: 0 errors (GREEN)
- tests: 4369 / 4369 passing (+19 new tests since baseline)
- lint: 242 errors, 518 warnings (matches baseline — Option A policy honored)
- build: GREEN (all 5 new API routes registered: /api/user-preferences,
  /api/user-preferences/lld, /api/lld/drill-attempts,
  /api/lld/drill-attempts/[id], /api/lld/drill-attempts/active)

## Plan deviations

1. **User-preferences schema created.** The plan referenced `userPreferences`
   from `@/db` in Task 8-9 but the schema did not exist in the repo. Created
   `architex/src/db/schema/user-preferences.ts` with `userId` PK, `preferences`
   JSONB, `createdAt`, `updatedAt`. Added to index + relations. Documented
   the `lld` subtree shape in the file header per Task plan guidance.
2. **Drizzle baseline migration committed.** Task 2 expected an incremental
   migration file, but `drizzle/migrations/` was empty so `db:generate`
   produced a full baseline migration (0000_skinny_callisto.sql). The user-
   preferences schema was added to produce a separate 0001_nostalgic_rattler.sql.
3. **`useHint` polymorphic.** Plan asked for `useHint(tier: HintTier) => void`
   in interview-store, but an existing `useHint()` with different semantics
   was already live and tested. Made the argument optional and polymorphic:
   when `tier` is supplied AND a drill is active, appends to `activeDrill.hintsUsed`;
   otherwise preserves legacy counter behavior. All 8 pre-existing tests keep
   passing; new drill test passes.
4. **`db:push` skipped.** No local Postgres connection in the agent context.
   Migration files are committed and will apply via `pnpm db:push` on a
   developer machine.
